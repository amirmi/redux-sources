{
  "name": "redux-sources",
  "version": "1.0.0",
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
    "transform-runtime"
    ]
  },
  "description": "Sources for Redux",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel --sourceMaps=true --watch src -d lib",
    "build": "babel --sourceMaps=true src -d lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-latest": "^6.16.0"
  }
}
